For the code to work start the server file first then the client file, or else you will encountre error

This was just to test my first commt using git commands, otherwaise the code is just a stupid teting fo it.